// sliding window === create a window ( array or number ) from one position to another
// depending on certain condition the window can increases or close ( and a new window is created )
// useful for: keeping track of subset of data in array/string etc

// often refers to subset of data

// write a function maxSubArraySum which accepts an array of integers and a number n and
// return the maximum sum of n consecutive elements in the array

function maxSubArraySum(array, n) {
    return;
}